import "package:flutter/material.dart";

import "Widgets/AddNewSpentFormViewBody.dart";

class AddNewSpentFormView extends StatelessWidget {
  const AddNewSpentFormView({super.key});

  @override
  Widget build(BuildContext context) {
    return const AddNewSpentFormViewBody();
  }
}
